[build]
  command = "mkdir -p public && cp -r index.html styles.css app.js config.json public/"
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
